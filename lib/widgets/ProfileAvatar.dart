import 'package:flutter/cupertino.dart';

class ProfiileAvatar extends StatelessWidget {
  final String imageUrl;
  final bool isActive;

  const ProfiileAvatar({Key key, @required this.imageUrl, this.isActive})
      : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
